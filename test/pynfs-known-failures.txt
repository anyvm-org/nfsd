# Known pynfs NFSv4.0 servertests failures for nfsd.py.
#
# This list is EMPTY: nfsd.py currently passes every applicable test of the
# pynfs NFSv4.0 servertests suite (589 passed, 0 failed, 2 warned,
# 10 skipped of 601 selected; pynfs master, verified in CI 2026-07-12).
#
# The 2 warnings are POSIX-advisory (e.g. LOCK4 write-locking a read-mode
# file) and the 10 skips are tests the suite itself deems inapplicable.
#
# test/conformance.sh fails CI when a test NOT in this list fails
# (regression), and prints a note when a listed test starts passing
# (tighten this list by removing it). If a genuine, accepted gap ever
# appears, add its test code (one per line) with a comment explaining why.
