# Known pynfs NFSv4.1 servertests failures for nfsd.py.
#
# nfsd.py implements NFSv4.1 with sessions but WITHOUT a backchannel
# (no callbacks), therefore it never grants delegations. Everything else
# a server can answer on the fore channel is implemented, including the
# strict EXCHANGE_ID/CREATE_SESSION record state machines, session limit
# enforcement (REQ_TOO_BIG/REP_TOO_BIG/TOO_MANY_OPS), the current
# stateid, RECLAIM_COMPLETE gating, and OPEN_DELEGATE_NONE_EXT replies.
#
# Standing: 172 passed / 12 failed / 0 skipped of 184 selected
# (pynfs master, 2026-07-12).
#
# test/conformance.sh (MINOR=1) fails CI when a test NOT in this list
# fails (regression), and prints a note when a listed test starts passing
# (tighten this list by removing it).

# delegations: granting one requires a backchannel to recall it with,
# and nfsd.py runs fore-channel only by design
DELEG1
DELEG2
DELEG3
DELEG5
DELEG6
DELEG7
DELEG8
DELEG23
DELEG26

# delegation callback tests also needing FATTR4_OPEN_ARGUMENTS (an
# attribute newer than RFC 5661/5662)
DELEG24
DELEG25

# pynfs's own XDR library cannot encode the undefined opnum 0 this test
# wants to send; the request never reaches the server
COMP5
